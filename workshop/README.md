# Workshop Local Prototype

This folder contains the Sprint 1 local prototype for The Workshop.

The prototype is intentionally simple:

- local folders for project organization
- JSON for structured data
- Markdown for readable outputs
- one first fixture project: The Myr Singularity

Sprint 1 should prove the product loop before building a web UI, database, automatic recommendation engine, or advanced simulation system.
